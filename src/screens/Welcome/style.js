// const { StyledFormArea, SubTitle, StyledButton, ButtonText, Line, WelcomeContainer, Avatar, WelcomeImage } = styles
import { StyleSheet } from 'react-native';
// import { H} from ''

export const style = StyleSheet.create({
    WelcomeImage: {
        height: '50%',
        borderRadius: 200,
    },
    WelcomeContainer: {
        padding: 25,
        paddingTop: 10,
        justifyContent: 'center',
    },
});
